import { writable } from 'svelte/store';

export const scrollDistance = writable(0);
export const headerHeight = writable(0);
export const navHeight = writable(0);
export const selectedChapter = writable(0);
